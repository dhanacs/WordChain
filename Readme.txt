This folder contains the solution to Code Kata problem 19. Assumed all dictionary words to be of same length w. 
This solution can be extended to arbitrary length dictionary words.

Other Approach : Strings can be "hashed" without using "tries". Adjacency matrix can then be constructed.

Related Problem : List all the anagrams of a given word. Can be solved efficiently using hashing.
